version: 2

sources:
  - name: raw
    description: Jaffle Shop raw app tables (dbt-labs/jaffle_shop_duckdb)
    schema: raw
    tables:
      - name: raw_orders
        description: Raw order records
      - name: raw_customers
        description: Raw customer records
