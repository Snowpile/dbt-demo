# Keep agent context small: ignore heavy build / data trees (not a security file).
# Repo policy for agents: AGENTS.md + .agents/skills/

target/
dbt_packages/
logs/
state/
.venv/
venv/
data/*
!data/seeds/
!data/seeds/**
*.parquet
*.csv
*.duckdb
*.duckdb.wal
.git/
