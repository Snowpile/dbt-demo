{% docs marketing_fct_customer_orders %}
Incremental order-grain fact for marketing (customer × order). Uses `delete+insert`
on `order_id`. Shared column descriptions live in `models/docs/*.md`.
{% enddocs %}
