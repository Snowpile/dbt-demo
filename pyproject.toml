[project]
name = "benderik"
version = "0.1.0"
description = "Data engineering — dbt (DuckDB) multi-domain"
readme = "projects/README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-duckdb>=1.9.0",
    "duckdb>=1.0.0",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.8.0",
]
