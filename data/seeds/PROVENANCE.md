# Seed data provenance

| File | Source | License |
|------|--------|---------|
| `raw_customers.csv` | [dbt-labs/jaffle_shop_duckdb](https://github.com/dbt-labs/jaffle_shop_duckdb) `duckdb` branch | Apache 2.0 |
| `raw_orders.csv` | same | Apache 2.0 |
| `raw_payments.csv` | same | Apache 2.0 |

Official dbt Labs fictional ecommerce demo. SHA-256 pins in `checksums.sha256`. Run `scripts/scan_downloads.sh` before every load.
